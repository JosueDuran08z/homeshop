import 'package:flutter/material.dart';

class Gestionar extends StatefulWidget {
  const Gestionar({Key? key}) : super(key: key);

  @override
  _GestionarState createState() => _GestionarState();
}

class _GestionarState extends State<Gestionar> {
  @override
  Widget build(BuildContext context) {
    return Center(child: const Text("Gestionar"));
  }
}
